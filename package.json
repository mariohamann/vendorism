{
  "name": "vendorism",
  "version": "3.0.0",
  "description": "",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "generate-types": "find . -name '*.d.ts' -delete && npx -p typescript tsc src/*.js --declaration --allowJs --emitDeclarationOnly --outDir ./src"
  },
  "keywords": [],
  "author": "mariohamann",
  "license": "ISC",
  "dependencies": {
    "@guoyunhe/downloader": "^1.3.0",
    "dependency-tree": "^10.0.9",
    "globby": "^13.2.2"
  },
  "devDependencies": {
    "vitest": "^0.34.6"
  }
}
